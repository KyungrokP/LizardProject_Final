# Core pipeline
numpy>=1.26
pandas>=2.2
pyyaml>=6.0
typer>=0.12

# TAPNext local-torch backend
torch>=2.2
torchvision>=0.17
einops>=0.7
timm>=1.0
# Note: TAPNext runtime also requires an installed `tapnet` package that
# provides `tapnet.tapnext.*` modules (install from your TAPNext source setup).

# DeepLabCut
deeplabcut>=3.0.0rc13

# Testing
pytest>=8.2
